app = "portfolio-web"
primary_region = "sin"

[build]
  image = "oven/bun:1.2-alpine"

[env]
  PORT = "3000"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0
